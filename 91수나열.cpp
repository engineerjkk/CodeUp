#include <stdio.h>

int main() {
    int a, b, c;
    int i;
    scanf_s("%d %d %d", &a, &b, &c);
    while (1) {
        day++;
        if ((day % a == 0) && (day % b == 0) && (day % c) == 0))
        printf("%d", day);
        break;
    }
 
    return 0;
}
